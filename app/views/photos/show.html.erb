<div class="container-home"><br><br>
  <%= link_to "Back to All Photos", photos_path, class: "btn btn-lg btn-default btn-home" %>
  <div class="row">
    <div class="col-md-4"></div>
      <div class="image-show rounded col-md-4"><%= image_tag(@image) %></div><br>
      <div class="col-md-4"></div>

      <div class="row">
        <div class="image-comments col-md-4 col-md-offset-4">
          <% @comments.each do |comment| %>
            " <%= comment %> "<br><br>
          <% end %>
      </div>
    </div>
  </div>
</div>
